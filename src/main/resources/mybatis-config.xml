<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
        <!-- 关闭缓存提高性能 -->
        <setting name="cacheEnabled" value="false"/>
        <!-- 自动生成主键 -->
        <setting name="useGeneratedKeys" value="true"/>
        <!-- 默认执行器 （SIMPLE、REUSE、BATCH） -->
        <setting name="defaultExecutorType" value="REUSE"/>
        <!-- 驼峰命名匹配 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <!-- 别名 -->
    <typeAliases>
        <typeAlias alias="HydrologicalInfo" type="com.typeng.hydrology.model.HydrologicalInfo"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!-- 配置数据库连接信息 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url"
                          value="jdbc:mysql://localhost:3306/hydrological_information?useUnicode=true&amp;characterEncoding=UTF-8"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/HnswMapper.xml"/>
    </mappers>

</configuration>